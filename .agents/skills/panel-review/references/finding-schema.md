---
name: panelist-receipt-compatibility
---

# Panelist receipt compatibility

This path is not a finding contract. The authoritative, machine-readable
contract is `../assets/panelist-receipt.schema.json`. The orchestrator loads
and passes that schema directly.
