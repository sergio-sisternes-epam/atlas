---
name: synthesizer-receipt-compatibility
---

# Synthesizer receipt compatibility

This path is not a synthesizer contract. The authoritative, machine-readable
contract is `../assets/synthesizer-receipt.schema.json`; the external output
shape is `../assets/recommendation-template.md`. The orchestrator loads
those assets directly.
