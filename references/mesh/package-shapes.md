# Package shapes (MVP)

1. **Embedded.** Skill or project repo. OKF root at `references/atlas`. Mesh `subpath` records that folder.
2. **Dedicated.** The repository is the Atlas. `subpath` empty.

APM may copy files. Mesh membership still requires `atlas mount` of a git identity.
